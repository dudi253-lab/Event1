'use client';import {useEffect,useState} from 'react';type P={id:string;url:string;status:string};
export default function ModeratorClient({slug}:{slug:string}){const [event,setEvent]=useState<{id:string;name:string}|null>(null),[pin,setPin]=useState(''),[ready,setReady]=useState(false),[photos,setPhotos]=useState<P[]>([]),[error,setError]=useState('');useEffect(()=>{fetch(`/api/events/${slug}`).then(r=>r.json()).then(setEvent)},[slug]);const load=async(id:string)=>{const r=await fetch(`/api/media?eventId=${id}&status=pending`);if(r.ok)setPhotos((await r.json()).photos||[])};const login=async()=>{if(!event)return;const r=await fetch('/api/staff/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({eventId:event.id,pin})});if(!r.ok){setError((await r.json()).error);return}setReady(true);setError('');await load(event.id)};const moderate=async(photoId:string,status:string)=>{if(!event)return;const r=await fetch('/api/staff/moderate',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({eventId:event.id,photoId,status,expectedStatus:'pending'})});if(r.ok)setPhotos(v=>v.filter(p=>p.id!==photoId));else setError((await r.json()).error)};if(!event)return <main className="center">טוען…</main>;if(!ready)return <main className="center card"><div className="brand">digi</div><h1>{event.name}</h1><input className="pin" inputMode="numeric" type="password" maxLength={4} value={pin} onChange={e=>setPin(e.target.value.replace(/\D/g,'').slice(0,4))}/><button className="button dark" onClick={()=>void login()}>כניסה</button>{error&&<p>{error}</p>}</main>;return <main className="panel"><h1>ממתינות לאישור</h1>{photos.map(p=><article key={p.id}><img src={p.url} alt="תמונה ממתינה"/><div><button onClick={()=>void moderate(p.id,'approved')}>אישור</button><button onClick={()=>void moderate(p.id,'private')}>פרטי</button><button onClick={()=>void moderate(p.id,'rejected')}>דחייה</button></div></article>)}{!photos.length&&<p>אין תמונות ממתינות.</p>}</main>}
